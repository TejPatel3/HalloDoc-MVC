
<div class="container mt-5">

    <div class="d-flex justify-content-between align-items-center mt-2">
        <div class="d-flex">
            <h4>Patient Record</h4>
        </div>
        <div>
            <a asp-controller="Records" asp-action="PatientHistory"
               class="btn m-0 btn-outline-info  px-2 ">
                <i class="fa-solid fa-chevron-left"></i>
                Back
            </a>
        </div>
    </div>
    <div class="container-fluid table-responsive">
        <table>
            <thead class="bg-secondary text-white">
                <tr>
                    <td style="width:12%">Client/Member</td>
                    <td style="width:12%">Created Date</td>
                    <td style="width:16%">Confirmation</td>
                    <td style="width:13%">Provider Name</td>
                    <td style="width:39%">Concluded Date</td>
                    <td style="width:8%">Status</td>
                    <td style="width:8%">Final Report</td>
                    <td style="width:8%">Actions</td>
                </tr>
            </thead>
            <tbody>
               @*  @foreach (var item in Model.UserList)
                {
                    <tr>
                        <td>@item.FirstName</td>
                        <td>@item.LastName</td>
                        <td>@item.Email</td>
                        <td>@item.Mobile</td>
                        <td>@item.Street @item.City @item.State</td>
                        <td>
                            <button class="btn btn-outline-info">Explore</button>
                        </td>
                    </tr>
                } *@
            </tbody>
        </table>
    </div>

</div>